package com.eps.android.ui.theme

import androidx.compose.ui.graphics.Color

// Futuristic Palette
// Void Black Theme
val VoidBlack = Color(0xFF050505)
val CyberDark = Color(0xFF0A0F14)
val PanelGlass = Color(0xFF11161C)

// Neon Accents (High Energy)
val ElectricCyan = Color(0xFF00FFFF)
val LaserRed = Color(0xFFFF0033)
val PlasmaYellow = Color(0xFFFFD700)
val MatrixGreen = Color(0xFF00FF41)

// Gradients & Effects
val ShieldGradientStart = Color(0xFF00C6FF)
val ShieldGradientEnd = Color(0xFF0072FF)
val DangerGradientStart = Color(0xFFFF416C)
val DangerGradientEnd = Color(0xFFFF4B2B)

// Typography Colors
val TextHolo = Color(0xFFE0F7FA)
val TextMuted = Color(0xFF546E7A)

// Glass Opacities
val GlassStroke = Color(0xFFFFFFFF).copy(alpha = 0.15f)
// val GlassSurface = Color(0xFF000000).copy(alpha = 0.6f) // Deprecated in favor of Onyx variant

// --- GUARDIAN GOLD THEME ---
val OnyxBlack = Color(0xFF030508)
val GoldPremium = Color(0xFFFFD700)
val GoldMuted = Color(0xFFB8860B)
val GlassSurface = Color(0xFF1A1F26).copy(alpha = 0.7f)
val AmbientGold = Color(0xFFFFD700).copy(alpha = 0.1f)
